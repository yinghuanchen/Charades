export default class Game {
    constructor(canvas) { 

        this.restart();
    } 

    restart() {
        
        // ProgressCountdown(60).then(value => alert(`Page has started: ${value}.`));
    }
}